require 'spec_helper'

describe Foxytools do
  it 'has a version number' do
    expect(Foxytools::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
